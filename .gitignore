# Logs
logs
# backend'in logs kaynak modülü runtime log klasörü değil — ignore dışı
!backend/src/logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files
.env
.env.*
!.env.example

# User uploads (runtime files, not versioned)
backend/uploads/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.obsidian/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.tsbuildinfo

# Local review/todo scratch notes (not versioned)
YAPILACAKLAR-*.txt

# Local dev compose overrides (host port mappings etc.) — never deployed
docker-compose.override.yml
